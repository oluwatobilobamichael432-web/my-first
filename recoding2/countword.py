def count_word():
    sentence = input("Enter a complete sentence\n")
    count = 0
    sentence.split()
    for word in sentence:
        if word == word:
            count += 1
            return count

print(count_word())